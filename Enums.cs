namespace Vheos.Mods.UNSIGHTED
{
    internal enum SettingsPreset
    {
        Vheos_HardMode,
        Vheos_UI,
    }

    internal enum BindingConflictResolution
    {
        Swap,
        Unbind,
        Duplicate,
    }
}